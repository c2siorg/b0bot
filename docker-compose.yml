services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    restart: always

  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    depends_on:
      - redis
